
# Ask the user to enter a given sample area of width and length of the rectangle to solve the area.

width = float(input("Enter a given width of the rectangle: "))
length = float(input("Enter a given length of the rectangle: "))

area = width * length

print("Area of rectangle is = ",area)